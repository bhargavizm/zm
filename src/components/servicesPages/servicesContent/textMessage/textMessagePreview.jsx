'use client'

import React from 'react'

export default function TextMessagePreview({ data }) {
  return (
    // <div className="w-full lg:w-1/3 mt-10 lg:mt-0 border-2 border-gray-300 rounded-2xl p-4 shadow-lg bg-white max-w-xs mx-auto relative overflow-hidden"
    //   style={{ height: '700px', borderRadius: '40px' }}>
    //   <div className="absolute top-1 w-full text-center">
    //     <div className="w-20 h-2 bg-gray-400 rounded-full mx-auto my-3" />
    //   </div>

    //   <div className="mt-6 space-y-2 overflow-y-auto h-[630px] p-2">
    //     <h2 className="text-xl font-bold text-center underline">Preview</h2>

    //     {data.senderName && <p><strong>Sender Name:</strong> {data.senderName}</p>}
    //     {data.messageType && <p><strong>Message Type:</strong> {data.messageType}</p>}
    //     {data.textMessage && <p><strong>Text Message:</strong> {data.textMessage}</p>}
    //     {data.dateTime && <p><strong>Date & Time:</strong> {data.dateTime}</p>}
    //     {data.senderAddress && <p><strong>Sender Address:</strong> {data.senderAddress}</p>}
    //     {data.senderMap && <p><strong>Sender Map:</strong> {data.senderMap}</p>}
    //     {data.password && <p><strong>Password:</strong> {data.password}</p>}
    //   </div>
    // </div>

    <>
    <section>
        <h1>text message preview</h1>
    </section>
    </>
  )
}
